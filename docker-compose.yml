services:
  web:
    build: .
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - FILE_STORAGE_PATH=${FILE_STORAGE_PATH:-./workspace}
    depends_on:
      - terminal

  terminal:
    image: node:18-alpine
    volumes:
      - ${FILE_STORAGE_PATH:-./workspace}:/workspace
    working_dir: /workspace
    command: sh
    environment:
      - NODE_ENV=development
    tty: true
    stdin_open: true